load("vect");
express([dx, dy, 0] ~ [0, 0, 1]);

assume(y > 0);
factor(2 * %pi * integrate(exp(-z^2/d^2), z, 0, y));

f(y):=exp(-y^2)/%pi;
G(dr):=1/d^2 * f(dr/d);

define(g(y), 2*%pi*integrate(f(y)*y, y, 0, y));

G(dr);

g(dr/d), expand;
